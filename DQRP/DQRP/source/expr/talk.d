module expr.talking;

import std.array;
import std.stdio;
import std.stdint;
import std.string;
import std.zlib;

/**
 * The full expression of g() * 2 above is f() + g() * 2, but not the full expression of f() + g() because 
 * the latter is not parsed as a subexpression.
 *
 * Note: Although the definition is straightforward, a few subtleties exist related to function literals:*/

 interface talking {
          class QRP {
            public static void Link(const char *files, char names, short bpf, ubyte vsp,
            byte lsp, char input, short tt, ubyte green, char name, wchar rosk,
            ubyte button, byte image, long homeoffice, ubyte interval, byte call, 
            char cache, long flesh, wchar overflesh)(ref Overpass){
                 return (() => x + f())() * g();
            }
    }
 }
   
                  
