module expr.seq;

import std.algorithm;
import std.array;
import std.file;
import std.bigint;
import std.getopt;
import std.numeric;
import std.digest;

/**
 *  An expression is a sequence of operators and operands that specifies an evaluation. 
 * The syntax, order of evaluation, and semantics of expressions are as follows.
 *
 * Expressions are used to compute values with a resulting type. These values can then 
 * be assigned, tested, or ignored. Expressions can also have side effects. 
 */

interface Dlang {
        class QRP {
            public static void Digest(const char *digest, char *name, short link,
                   wchar bb, size_t tt, long geo, ubyte call, byte callf, short accept,
                   long bpf,  char mpl, short append)(ref AppendClick){

                    return f() + g() * 2;

                    }
        }
 }
 
    
